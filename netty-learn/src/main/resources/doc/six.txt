基于java提供的对象输入/输出流ObjectInputStream和ObjectoutputStream可以直接把java对象作为可存储的字节数组写入文件,也可以传输到网络
JDK默认的序列化机制可以避免操作底层的字节数组,提升开发效率
java序列化的目的:
    网络传输
    对象持久化
java序列化的缺点:
    无法跨语言
    序列化后的码流太大

序列化工具(编解码框架)主要考虑方向:
    是否跨语言
    编码后的码流大小
    编解码的性能
    类库是否小巧
    使用者需要手工开发的工作量和难度